# 项目需求上下文

本项目为制造业新产品开发项目的全生命周期管理系统，以门径管理模式为核心框架。项目阶段依次为 CKO、PKO、Gate1、Gate2~6、Hand Over，由销售人员、PM（项目经理）、AE（应用工程师）、SCM（供应链管理工程师）、AQE（质量工程师）、SQD（供应商质量开发工程师）跨职能协同执行，实现从项目创建到开发交接的全流程管控。

后续新开对话时，应优先理解并遵循本文中的业务流程、角色职责、阶段流转和最新已明确规则。

## 最新已明确规则摘要

项目开发和子零件开发是两个相互独立的功能模块。

项目开发模块负责项目主流程、项目 Gate 流转、项目级任务、项目级 BOM、L1 总成 APQP 和项目审批准入。

子零件开发模块负责子零件开发任务、供应商开发过程、子零件 APQP、子零件 Gate 或阶段进度，以及对应资料、问题和追溯。

子零件开发任务允许在子零件开发模块中手动创建，不要求全部来源于项目开发 BOM，也不要求必须由项目开发中“根据 BOM 创建 APQP”动作生成。

项目 BOM 中的某个子零件如果已经在子零件开发模块存在对应开发任务，项目 BOM 或项目 APQP 页面可以展示该子零件开发任务号；用户点击任务号时，应跳转至子零件开发任务详情。

子零件开发任务的 Gate、状态、完成情况、供应商开发状态、子零件 APQP 状态不再作为项目开发 Gate 流转的硬阻塞条件。项目 Gate 是否继续推进由项目团队人工结合风险、交付承诺、客户要求和审批意见判断。

系统可以在项目 Gate 审批前展示子零件开发风险、延期、未启动、待影响分析、供应商风险等信息，但只能作为风险提醒、协同参考和人工判断依据，不得描述为项目 Gate 阻塞原因。

## 一、CKO 阶段（项目启动前期）

流程由销售人员发起，首先进入“开始”节点。

销售人员执行“创建开发项目”操作，生成新的开发项目；接着完成 CKO 任务。CKO 任务为系统默认配置，支持超级管理员调整任务内容。

完成 CKO 任务后，销售人员提交项目，项目流转至 PKO 阶段。

## 二、PKO 阶段（项目启动阶段，Gate1 前）

项目进入 PKO 阶段后，由 PM 发起 PKO（项目启动会），Gate1 至 Gate6 的团队成员参与会议。

PKO 阶段需输入关键节点信息：

- 客户 OTS、PPAP、SOP 节点
- BW 内部 OTS、PPAP、SOP 节点

节点对应关系如下：

- BW 内部 OTS = Gate4
- BW 内部 PPAP = Gate5
- BW 内部 SOP = Gate6

PM 根据产品类型判断流程分支，分为标准流程和简化流程。

标准流程覆盖 Gate1 至 Gate6，适用系统包括：

- VT 系统：Phaser、VFS、CB、CCV
- TD 系统：Chain、TSC、Timing、Tensioner、OP、CC Guide、Arm Guide

简化流程覆盖 Gate1 至 Gate3，适用于：

- PP/Chain 内部子零件
- Motor Chain 场景

流程分支确定后，PM 向各团队成员分配待办任务，各角色按任务依次执行：

1. AE 接收待办任务后，执行“创建 BOM”操作，生成产品 BOM 初稿。
2. AE 对 BOM 信息进行确认与编辑，完成后提交并推送至 SCM。
3. SCM 接收 BOM 信息后，执行确认与编辑操作，完成后提交回 PM。

PM 接收 SCM 确认后的 BOM 信息，流程进入 Gate1 阶段。

## 三、Gate1 阶段（项目准入阶段）

Gate1 阶段由 PM、AQE、SQD 协同执行。

1. PM 根据 SCM 确认的 BOM 信息，执行“根据 BOM 创建总成 APQP”操作，完成后提交项目，项目进入 Gate2 阶段。
2. AQE 接收 PM 创建的总成 APQP 计划后，可基于 BOM 拆解总成下的子总成/子零件开发需求，并根据业务需要在子零件开发模块中创建或关联子零件开发任务。
3. SQD 在子零件开发模块中接收或维护子零件开发任务，对接对应供应商，管理子零件开发工作，并同步关键节点信息至系统。
4. 子零件开发任务属于独立模块，其创建、分配、Gate 状态和完成情况不作为项目从 Gate1 进入 Gate2 的硬性前置条件。

已明确规则：

- 项目进入下一 Gate 只受项目级任务、项目级输入、L1 总成 APQP 和项目审批状态约束。
- 不要求所有子零件开发任务完成分配后，项目才能进入下一环节；子零件开发情况由项目团队人工判断风险。

## 四、Gate2~6 阶段（项目开发阶段，多门径管控）

Gate2 至 Gate6 阶段为项目开发的多轮门径管控阶段，流程由 PM 主导推进。

1. PM 向各团队成员分配 Gate2 至 Gate6 对应的待办任务。
2. 各职能部门接收任务后，按要求推进工作，并反馈任务完成情况。
3. PM 组织团队对各 Gate 阶段的任务完成情况进行审批，审批通过后进入下一个 Gate 阶段。
4. 当 Gate2 至 Gate6 的所有任务全部完成后，PM 手动触发流程，项目流转至 Hand Over 阶段。

已明确规则：

- 不需要所有子零件的对应 Gate 任务全部完成后，项目才能进入下一 Gate 环节。
- 子零件 Gate 和项目 Gate 分属不同模块；子零件开发进度可在项目 Gate 审批时提示为风险或协同信息，但不作为项目 Gate 流转的系统硬阻塞。

## 五、Hand Over 阶段（项目交接阶段）

项目进入 Hand Over 阶段后，由 PM 执行“进行交接信息登记”操作，完成项目交接信息的闭环。

交接完成后，流程进入“结束”节点。

当前无新增待澄清规则。


项目 APQP 与子零件 APQP 逻辑关系（核心规则）
创建顺序：项目 BOM 创建并确认后，由 PM 创建 BOM 中 L1 层级的 APQP（L1 层级必须创建）。
负责人指定：创建 L1 APQP 时需指定 L1 层级负责人，默认 AQE。
子零件创建：子零件开发 APQP 属于子零件开发管理模块，可由 L1 层级负责人基于 BOM 创建，也可在子零件开发管理模块中手动新增零件开发任务；创建时需指定子零件负责人，默认 SQD。
任务展示：项目 APQP 仅展示项目 Gate 任务和 L1 总成 APQP 任务；子零件 APQP 可作为关联信息、风险提示或跳转入口展示，但不并入项目 Gate 的完成条件。
阶段推进条件：项目 Gate 阶段仅以项目开发任务和 L1 总成 APQP 的审批要求作为准入条件；子零件 APQP 的完成情况不影响项目 Gate 提交、审核和流转。

## 六、BOM、供应方案与子零件开发任务联动规则

### 1. 核心原则

项目开发任务与子零件开发任务是两个独立业务模块。项目开发任务负责项目 Gate 流转和项目 APQP 管控；子零件开发任务负责零件、供应商、子零件 APQP 和供应商开发过程管控。

项目 BOM 是子零件开发任务的重要来源之一，但不是唯一来源。子零件开发任务可通过 BOM 中的零件和供应方案创建，也可在子零件开发管理模块中手动新增。

项目 BOM 确认后，系统才允许基于 BOM 生成 L1 总成 APQP 和来源于 BOM 的子零件 APQP。BOM 未确认或仍处于草稿状态时，不应直接从该 BOM 生成正式子零件开发任务；但不影响在子零件开发管理模块中手动新增与 BOM 无强绑定关系的零件开发任务。

项目 Gate 审批仅受项目自身 Gate 任务、L1 总成 APQP、项目级必需输入和项目级审批状态约束。子零件开发任务、子零件 APQP 和供应商开发任务的状态不阻塞项目 Gate 流转，可作为项目风险、进度参考、资料追溯或跨模块提醒展示。

### 2. BOM 版本规则

项目 BOM 应采用“项目 BOM 容器 + L1 总成级 BOM 独立版本”的版本管理机制。

项目 BOM 容器只表示该项目下有哪些 L1 总成 BOM，不承载正式业务版本号，不应再作为项目整体 BOM 版本使用。

每一个 L1 总成应单独维护 BOM 版本链，区分草稿版本、待确认版本、已锁定生效版本、已作废版本和已冻结版本。

- 草稿版本：AE 或相关角色编辑中，不影响当前已生效任务。
- 待确认版本：AE 提交后等待 SCM 确认，不影响当前已生效任务。
- 已锁定生效版本：SCM 确认后成为该 L1 总成当前有效 BOM，可作为 L1 APQP、BOM 行追溯和来源于 BOM 的子零件开发任务生成依据。
- 已作废版本：被新版本替代后保留用于追溯。
- 已冻结版本：因业务需要暂停编辑或暂停生效流转，但仍保留历史。

同一个 L1 总成下，原则上同一时间只能有一个“已锁定生效”的 BOM 版本。

每个 L1 总成的 BOM 应独立变更、独立提交、独立确认、独立生效。某个 L1 的零件、供应商或规格变化，只生成该 L1 的新版本，不应导致项目下其他未变化 L1 同步升版。

L1 BOM 版本号建议采用“当前项目阶段 + 版本号”规则，例如 `PKO_V1.0`、`PKO_V2.0`。

AE 第一次创建 L1 BOM 时，只生成最新工作版本，不生成当前生效版本。SCM 确认后，系统才将该工作版本写为当前生效版本。

已生效 L1 BOM 再次编辑时，应从当前生效版本复制生成下一工作版本，例如当前生效版本为 `PKO_V1.0`，编辑后生成 `PKO_V2.0` 工作版本。SCM 确认后，当前生效版本与最新工作版本保持一致。

项目 BOM 列表应支持勾选待 SCM 确认的 L1 工作版本，批量提交或确认。确认动作只影响被勾选的 L1 BOM 版本。

历史版本不得被物理覆盖，应保留用于追溯。

### 3. BOM 行规则

BOM 行应支持完整层级结构，至少包括 L1 总成、子总成、子零件等层级。

每一条 BOM 行应具备以下业务属性：

- 零件编号
- 零件名称
- BOM 层级
- 父级零件
- 数量
- 产品应用
- 是否共用件
- 是否需要 APQP
- 是否需要生成子零件开发任务
- APQP 负责人
- 供应方案
- 起始 Gate
- 是否影响项目风险提示
- BOM 变更状态

不是所有 BOM 行都必须生成子零件开发任务。是否生成任务应由零件类型、是否共用件、是否新开发、是否关键件、供应策略等因素共同决定。

### 4. L1 APQP 与子零件 APQP 规则

L1 总成 BOM 确认后，PM 应基于该 L1 的已锁定生效 BOM 版本创建 L1 总成 APQP。L1 层级原则上必须创建 APQP。

创建 L1 APQP 时需指定负责人，默认负责人为 AQE。

L1 APQP 应关联到具体 L1 BOM 生效版本，而不是项目整体 BOM 版本。建议记录 `L1 APQP.sourceBomVersionId = AssemblyBomVersion.id`。

L1 负责人可基于 L1 BOM 版本下的 BOM 行继续拆解并创建子零件 APQP。创建子零件 APQP 时需指定子零件负责人，默认负责人为 SQD。

子零件 APQP 建议记录 `sourceBomLineId = BomLine.id`；供应商开发任务建议记录 `sourceSupplyPlanId = SupplyPlan.id`。

子零件 APQP 应归属于子零件开发管理模块，并可与项目、L1 总成 APQP、BOM 行和供应方案建立关联。项目 APQP 页面可展示子零件 APQP 的摘要或跳转入口，但不得将子零件 APQP 未完成作为项目 Gate 审批的阻塞条件。

BOM 行中的零件编号只有在该 BOM 行已创建 APQP 或对应零件开发任务后，才可作为跳转入口进入零件开发详情；AE 创建 BOM 或 SCM 确认 BOM 本身不代表所有零件均已生成 APQP 任务。

### 5. 一供、二供与供应方案规则

BOM 中的每个零件可以配置一个或多个供应商方案。一供表示首选供应商，二供表示备选供应商。

一供、二供不应仅作为普通文本字段处理，而应作为 BOM 行下的供应方案管理。

供应方案至少应包含以下属性：

- 供应商
- 供应商类型：一供 / 二供 / 其他
- 供应策略：单一供应 / 双供备案 / 双供同步开发 / 二供后续导入
- 是否需要开发
- 是否生成 APQP
- 是否影响项目风险提示
- 起始 Gate
- 默认负责人
- 关联 APQP 任务

供应策略规则如下：

- 单一供应：仅一供参与开发，默认生成一供子零件 APQP。
- 双供备案：二供仅作为备选备案，不生成 APQP，不影响项目 Gate。
- 双供同步开发：一供和二供均需生成子零件 APQP，默认纳入子零件开发管理模块重点跟踪，但不影响项目 Gate。
- 二供后续导入：二供在指定 Gate 或指定条件后启动开发；启动前仅备案，启动后按子零件开发任务处理，但不影响项目 Gate。

### 6. 子零件开发任务生成规则

子零件开发任务建议按“零件 + 供应商方案”生成，而不是仅按“零件”生成。

同一零件如果一供和二供都需要开发，应生成两条独立的子零件开发任务。

示例：

- 零件 P1，一供供应商 A，需开发：生成 P1-A 子零件开发任务。
- 零件 P1，二供供应商 B，仅备案：不生成 P1-B 子零件开发任务。
- 零件 P1，二供供应商 B，同步开发：生成 P1-B 子零件开发任务。

子零件开发任务支持两种创建来源：

- BOM 创建：从已确认 BOM 的 BOM 行和供应方案创建，自动记录来源 BOM 版本、BOM 行和供应方案。
- 手动新增：在子零件开发管理模块中由授权人员手动新增，适用于客户临时要求、供应商导入、试制验证、售后或其他不依赖当前项目 BOM 的零件开发场景。

手动新增的子零件开发任务可以关联项目，也可以作为独立零件开发任务管理；如后续与 BOM 建立关系，应通过关联或影响分析补充来源记录，不应反向阻塞项目 Gate。

子零件开发任务应继承或记录以下信息：

- 所属项目
- 创建来源：BOM 创建 / 手动新增
- 来源 BOM 版本（如有）
- 来源 BOM 行（如有）
- 零件信息
- 供应商信息
- 供应商类型
- 供应策略
- 负责人
- 起始 Gate
- 当前阶段或当前 Gate（用于子零件开发模块自身管理）
- 是否影响项目风险提示
- 当前状态

### 7. 项目 Gate 与子零件开发模块关系

项目 Gate 阶段审批提交前，系统必须进行准入检查。

项目当前 Gate 允许提交审批的前提是：

- 项目自身当前 Gate 任务已完成。
- 项目级必需输入已完成，例如关键节点、项目成员、项目 APQP 或 L1 总成 APQP 等。
- 项目级审批、驳回整改或条件通过事项已闭环。
- 与项目 Gate 直接相关的项目级 BOM 输入已达到项目审批所需状态。

若任一条件不满足，项目不得提交当前 Gate 审批。

子零件开发任务、子零件 APQP、供应商开发任务的状态不作为项目 Gate 审批准入条件。即使子零件开发任务未完成、供应商开发任务未完成、子零件 APQP 未关闭，项目 Gate 仍可按项目开发流程提交和审批。

系统可以在项目 Gate 审批前展示子零件开发风险摘要，例如未启动、延期、待影响分析、供应商风险等，但提示性质为风险提醒和跨模块协同，不得作为项目 Gate 的硬阻塞。

### 8. BOM 变更规则

项目执行过程中，BOM 可以在不同 Gate 阶段发生变化，但 BOM 变化不应直接破坏已生成的子零件开发任务。

BOM 变化应通过版本差异和影响分析驱动任务同步。

BOM 变更类型包括：

- 新增零件
- 删除零件
- 替换零件
- 一供变更
- 二供新增
- 二供删除
- 二供策略变化
- 供应商升降级，例如二供升为一供
- 数量变化
- 层级变化
- 规格或图纸状态变化

BOM 变更处理原则如下：

- 新增零件：判断是否需要生成子零件开发任务，并确定子零件开发起始阶段或起始 Gate。
- 删除零件：不得物理删除已生成任务，应关闭或取消任务并保留历史。
- 替换零件：应建立旧零件与新零件的替换关系，并判断任务是否继承、部分重做或完全重做。
- 一供变更：通常视为高影响变更，应触发供应商开发任务重评。
- 新增二供：根据二供策略判断是否生成二供开发任务。
- 二供删除：关闭或取消二供对应任务，保留历史。
- 二供备案改同步开发：应从指定 Gate 创建或启动二供开发任务。
- 数量变化：默认仅记录变更，必要时通知 SCM，不默认重建 APQP。
- 层级变化：应重新计算父子关系，并判断是否影响 APQP 展示和任务归属。
- 规格或图纸变化：应触发影响分析，必要时重开相关 Gate 任务。

### 9. BOM 变更对项目 Gate 的影响规则

项目 Gate 审批时，应聚合检查项目下所有涉及当前 Gate 的 L1 BOM 当前生效版本和未处理变更。只有明确影响项目 Gate 必需输入、L1 总成 APQP 或项目审批依据的 L1 BOM 变更，才可阻塞项目 Gate；仅影响子零件开发、供应商开发或子零件 APQP 的变更，不阻塞项目 Gate。

以下情况默认阻塞项目 Gate：

- 涉及当前 Gate 的 L1 总成 BOM 尚无已锁定生效版本。
- L1 BOM 存在草稿或待 SCM 确认版本，且该版本被标记为影响当前 Gate 审批依据。
- L1 BOM 变更导致项目 Gate 所需的 L1 总成 APQP、项目范围、关键节点或客户交付承诺无法确认。
- L1 BOM 变更被项目 PM 或审批人明确标记为项目级阻塞，且阻塞原因未关闭。

以下情况默认不阻塞项目 Gate：

- 子零件开发任务未创建、未启动、进行中或未完成。
- 子零件 APQP 未创建、进行中或未关闭。
- 供应商开发任务未完成。
- 新增零件仅影响子零件开发任务创建判断。
- 删除零件仅影响子零件开发任务关闭或取消。
- 替换零件仅影响子零件开发任务继承、重做或关闭判断。
- 一供或二供变更仅影响供应商开发任务重评。
- 二供从备案变为同步开发但仅影响二供子零件开发任务创建。
- 二供仅备案，且未被标记为需开发。
- 数量变化已记录且确认不影响开发。
- 文本类信息修正已确认不影响开发。
- 非关键属性变化已确认不影响开发。

### 10. 任务状态与追溯规则

子零件开发任务状态至少应包含：

- 未启动
- 进行中
- 当前 Gate 待完成
- 当前 Gate 已完成
- 待影响分析
- 变更待确认
- 已冻结
- 已取消
- 已完成

BOM 行变更状态至少应包含：

- 无变更
- 新增待确认
- 删除待确认
- 替换待确认
- 供应商变更待确认
- 已确认
- 已取消
- 已生效

所有 BOM 版本、BOM 行变更、供应商方案变化、APQP 任务生成、手动新增子零件开发任务、任务关闭、任务继承、项目 Gate 审批判断均应保留操作记录，支持后续追溯。

## 七、全局 UI 弹窗规则

项目中所有用户可见弹窗、确认提示、错误提示、成功提示、表单弹窗、详情弹窗、审批弹窗、删除确认和业务阻塞提示，均不得使用浏览器默认弹窗，包括 `alert`、`confirm`、`prompt`。

所有弹窗必须使用项目 UI 规范中的统一弹窗组件或统一样式结构，并保持视觉、交互和文案风格一致。

弹窗应至少满足以下规则：

- 使用统一遮罩、弹窗容器、标题区、内容区和底部操作区。
- 主操作、取消操作、危险操作的按钮样式保持一致。
- 删除、驳回、关闭任务等危险操作必须使用明确的二次确认弹窗。
- 表单类弹窗必须提供字段校验提示，不使用浏览器原生校验弹窗作为主要提示。
- 业务阻塞提示应说明阻塞原因和需要处理的对象，例如未处理的项目级 BOM 变更、未闭环的项目 Gate 任务、未确认的项目级审批事项。子零件开发任务和供应商开发任务只能作为风险提醒或跨模块协同提示，不得描述为项目 Gate 阻塞原因。
- 弹窗和业务列表中的表格字段应完整展示业务内容，默认不得使用 `...`、省略号或 `text-overflow: ellipsis` 截断关键字段；字段较长时应通过横向滚动、合理列宽、自动换行或详情展开承载完整信息。尤其是 BOM、APQP、变更记录、审批记录、任务清单等追溯类表格，必须保证用户可直接查看完整字段值和说明。
- 弹窗关闭方式、遮罩点击行为、关闭按钮位置在项目内保持一致。
- 后续新增页面或修改现有页面时，应同步替换已有浏览器默认弹窗。
