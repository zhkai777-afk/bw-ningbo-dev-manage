# 项目需求上下文

本项目为制造业新产品开发项目的全生命周期管理系统，以门径管理模式为核心框架。项目阶段依次为 CKO、PKO、Gate1、Gate2~6、Hand Over，由销售人员、PM（项目经理）、AE（应用工程师）、SCM（供应链管理工程师）、AQE（质量工程师）、SQD（供应商质量开发工程师）跨职能协同执行，实现从项目创建到开发交接的全流程管控。

后续新开对话时，应优先理解并遵循本文中的业务流程、角色职责、阶段流转和待澄清规则。

## 一、CKO 阶段（项目启动前期）

流程由销售人员发起，首先进入“开始”节点。

销售人员执行“创建开发项目”操作，生成新的开发项目；接着完成 CKO 任务。CKO 任务为系统默认配置，支持超级管理员调整任务内容。

完成 CKO 任务后，销售人员提交项目，项目流转至 PKO 阶段。

## 二、PKO 阶段（项目启动阶段，Gate1 前）

项目进入 PKO 阶段后，由 PM 发起 PKO（项目启动会），Gate1 至 Gate6 的团队成员参与会议。

PKO 阶段需输入关键节点信息：

- 客户 OTS、PPAP、SOP 节点
- BW 内部 OTS、PPAP、SOP 节点

节点对应关系如下：

- BW 内部 OTS = Gate4
- BW 内部 PPAP = Gate5
- BW 内部 SOP = Gate6

PM 根据产品类型判断流程分支，分为标准流程和简化流程。

标准流程覆盖 Gate1 至 Gate6，适用系统包括：

- VT 系统：Phaser、VFS、CB、CCV
- TD 系统：Chain、TSC、Timing、Tensioner、OP、CC Guide、Arm Guide

简化流程覆盖 Gate1 至 Gate3，适用于：

- PP/Chain 内部子零件
- Motor Chain 场景

流程分支确定后，PM 向各团队成员分配待办任务，各角色按任务依次执行：

1. AE 接收待办任务后，执行“创建 BOM”操作，生成产品 BOM 初稿。
2. AE 对 BOM 信息进行确认与编辑，完成后提交并推送至 SCM。
3. SCM 接收 BOM 信息后，执行确认与编辑操作，完成后提交回 PM。

PM 接收 SCM 确认后的 BOM 信息，流程进入 Gate1 阶段。

## 三、Gate1 阶段（项目准入阶段）

Gate1 阶段由 PM、AQE、SQD 协同执行。

1. PM 根据 SCM 确认的 BOM 信息，执行“根据 BOM 创建总成 APQP”操作，完成后提交项目，项目进入 Gate2 阶段。
2. AQE 接收 PM 创建的总成 APQP 计划后，根据 BOM 拆解总成下的子总成/子零件开发任务，分配给 SQD。
3. SQD 接收 AQE 分配的子零件开发任务后，对接对应供应商，管理子零件开发工作，并同步关键节点信息至系统。

待澄清规则：

- 是否需所有总成或子零件的任务分配完成后，才能进入下一环节？

## 四、Gate2~6 阶段（项目开发阶段，多门径管控）

Gate2 至 Gate6 阶段为项目开发的多轮门径管控阶段，流程由 PM 主导推进。

1. PM 向各团队成员分配 Gate2 至 Gate6 对应的待办任务。
2. 各职能部门接收任务后，按要求推进工作，并反馈任务完成情况。
3. PM 组织团队对各 Gate 阶段的任务完成情况进行审批，审批通过后进入下一个 Gate 阶段。
4. 当 Gate2 至 Gate6 的所有任务全部完成后，PM 手动触发流程，项目流转至 Hand Over 阶段。

待澄清规则：

- 是否需所有子零件的对应 Gate 任务全部完成后，才能进入下一 Gate 环节？

## 五、Hand Over 阶段（项目交接阶段）

项目进入 Hand Over 阶段后，由 PM 执行“进行交接信息登记”操作，完成项目交接信息的闭环。

交接完成后，流程进入“结束”节点。

待澄清规则：


项目 APQP 与子零件 APQP 逻辑关系（核心规则）
创建顺序：项目 BOM 创建并确认后，由 PM 创建 BOM 中 L1 层级的 APQP（L1 层级必须创建）。
负责人指定：创建 L1 APQP 时需指定 L1 层级负责人，默认 AQE。
子零件创建：L1 层级负责人可创建子零件 APQP，创建时需指定子零件负责人，默认 SQD。
任务展示：L1 及其子零件的 Gate 阶段任务，统一展示在项目 APQP 的 Gate 阶段任务下。
阶段推进条件：Gate 阶段下的全部任务完成（包括 L1 及其子零件）后，才可提交审核；审核通过后进入下一个 Gate 阶段。

## 六、BOM、供应方案与子零件开发任务联动规则

### 1. 核心原则

项目开发任务是主流程，项目 BOM 是子零件开发任务和 APQP 任务的主要来源。

项目 BOM 确认后，系统才允许基于 BOM 生成 L1 总成 APQP 和子零件 APQP。BOM 未确认或仍处于草稿状态时，不应直接生成正式子零件开发任务。

项目 Gate 审批必须受项目自身 Gate 任务、BOM 状态、子零件开发任务状态共同约束。当前 Gate 下存在未完成的阻塞型子零件任务，或存在未处理的 BOM 变更时，项目不得提交进入下一 Gate 的审批。

### 2. BOM 版本规则

项目 BOM 应采用版本管理机制，区分草稿版本、待确认版本和已锁定生效版本。

- 草稿版本：AE 或相关角色编辑中，不影响当前已生效任务。
- 待确认版本：AE 提交后等待 SCM 确认，不影响当前已生效任务。
- 已锁定生效版本：SCM 确认后成为当前有效 BOM，可作为 APQP 和子零件开发任务生成依据。

BOM 每次被 SCM 确认并锁定后，应形成新的 BOM 版本。历史版本不得被物理覆盖，应保留用于追溯。

### 3. BOM 行规则

BOM 行应支持完整层级结构，至少包括 L1 总成、子总成、子零件等层级。

每一条 BOM 行应具备以下业务属性：

- 零件编号
- 零件名称
- BOM 层级
- 父级零件
- 数量
- 产品应用
- 是否共用件
- 是否需要 APQP
- 是否需要生成子零件开发任务
- APQP 负责人
- 供应方案
- 起始 Gate
- 是否阻塞项目 Gate
- BOM 变更状态

不是所有 BOM 行都必须生成子零件开发任务。是否生成任务应由零件类型、是否共用件、是否新开发、是否关键件、供应策略等因素共同决定。

### 4. L1 APQP 与子零件 APQP 规则

BOM 确认后，PM 应基于 BOM 中 L1 层级创建 L1 总成 APQP。L1 层级原则上必须创建 APQP。

创建 L1 APQP 时需指定负责人，默认负责人为 AQE。

L1 负责人可基于 BOM 继续拆解并创建子零件 APQP。创建子零件 APQP 时需指定子零件负责人，默认负责人为 SQD。

L1 及其子零件的 Gate 阶段任务，应统一展示在项目 APQP 的对应 Gate 阶段任务下。

### 5. 一供、二供与供应方案规则

BOM 中的每个零件可以配置一个或多个供应商方案。一供表示首选供应商，二供表示备选供应商。

一供、二供不应仅作为普通文本字段处理，而应作为 BOM 行下的供应方案管理。

供应方案至少应包含以下属性：

- 供应商
- 供应商类型：一供 / 二供 / 其他
- 供应策略：单一供应 / 双供备案 / 双供同步开发 / 二供后续导入
- 是否需要开发
- 是否生成 APQP
- 是否阻塞项目 Gate
- 起始 Gate
- 默认负责人
- 关联 APQP 任务

供应策略规则如下：

- 单一供应：仅一供参与开发，默认生成一供子零件 APQP。
- 双供备案：二供仅作为备选备案，不生成 APQP，不阻塞项目 Gate。
- 双供同步开发：一供和二供均需生成子零件 APQP，且默认均阻塞项目 Gate。
- 二供后续导入：二供在指定 Gate 或指定条件后启动开发；启动前不阻塞项目 Gate，启动后按阻塞型任务处理。

### 6. 子零件开发任务生成规则

子零件开发任务建议按“零件 + 供应商方案”生成，而不是仅按“零件”生成。

同一零件如果一供和二供都需要开发，应生成两条独立的子零件开发任务。

示例：

- 零件 P1，一供供应商 A，需开发：生成 P1-A 子零件开发任务。
- 零件 P1，二供供应商 B，仅备案：不生成 P1-B 子零件开发任务。
- 零件 P1，二供供应商 B，同步开发：生成 P1-B 子零件开发任务。

子零件开发任务应继承或记录以下信息：

- 所属项目
- 来源 BOM 版本
- 来源 BOM 行
- 零件信息
- 供应商信息
- 供应商类型
- 供应策略
- 负责人
- 起始 Gate
- 当前 Gate
- 是否阻塞项目 Gate
- 当前状态

### 7. 项目 Gate 与子零件 Gate 联动规则

项目 Gate 阶段审批提交前，系统必须进行准入检查。

项目当前 Gate 允许提交审批的前提是：

- 项目自身当前 Gate 任务已完成。
- 当前生效 BOM 不存在未处理的变更。
- 当前 Gate 下所有阻塞型子零件开发任务已完成。
- 新增零件已生成对应开发任务，或已明确标记为无需开发。
- 删除零件对应开发任务已关闭或取消。
- 替换零件已完成继承、重做或关闭判断。
- 供应商变更已完成影响确认。

若任一条件不满足，项目不得提交当前 Gate 审批。

### 8. BOM 变更规则

项目执行过程中，BOM 可以在不同 Gate 阶段发生变化，但 BOM 变化不应直接破坏已生成的子零件开发任务。

BOM 变化应通过版本差异和影响分析驱动任务同步。

BOM 变更类型包括：

- 新增零件
- 删除零件
- 替换零件
- 一供变更
- 二供新增
- 二供删除
- 二供策略变化
- 供应商升降级，例如二供升为一供
- 数量变化
- 层级变化
- 规格或图纸状态变化

BOM 变更处理原则如下：

- 新增零件：判断是否需要生成子零件开发任务，并确定起始 Gate。
- 删除零件：不得物理删除已生成任务，应关闭或取消任务并保留历史。
- 替换零件：应建立旧零件与新零件的替换关系，并判断任务是否继承、部分重做或完全重做。
- 一供变更：通常视为高影响变更，应触发供应商开发任务重评。
- 新增二供：根据二供策略判断是否生成二供开发任务。
- 二供删除：关闭或取消二供对应任务，保留历史。
- 二供备案改同步开发：应从指定 Gate 创建或启动二供开发任务。
- 数量变化：默认仅记录变更，必要时通知 SCM，不默认重建 APQP。
- 层级变化：应重新计算父子关系，并判断是否影响 APQP 展示和任务归属。
- 规格或图纸变化：应触发影响分析，必要时重开相关 Gate 任务。

### 9. BOM 变更对 Gate 的阻塞规则

当前生效 BOM 存在未处理变更时，项目 Gate 审批应被阻塞。

以下情况默认阻塞项目 Gate：

- 新增零件未完成是否开发判断。
- 新增零件需要开发但尚未生成子零件开发任务。
- 删除零件对应任务未关闭。
- 替换零件未完成影响分析。
- 一供变更未完成确认。
- 二供从备案变为同步开发但任务未创建。
- 当前 Gate 下存在未完成的阻塞型一供或二供开发任务。

以下情况默认不阻塞项目 Gate：

- 二供仅备案，且未被标记为需开发。
- 数量变化已记录且确认不影响开发。
- 文本类信息修正已确认不影响开发。
- 非关键属性变化已确认不影响开发。

### 10. 任务状态与追溯规则

子零件开发任务状态至少应包含：

- 未启动
- 进行中
- 当前 Gate 待完成
- 当前 Gate 已完成
- 待影响分析
- 变更待确认
- 已冻结
- 已取消
- 已完成

BOM 行变更状态至少应包含：

- 无变更
- 新增待确认
- 删除待确认
- 替换待确认
- 供应商变更待确认
- 已确认
- 已取消
- 已生效

所有 BOM 版本、BOM 行变更、供应商方案变化、APQP 任务生成、任务关闭、任务继承、Gate 审批判断均应保留操作记录，支持后续追溯。

## 七、全局 UI 弹窗规则

项目中所有用户可见弹窗、确认提示、错误提示、成功提示、表单弹窗、详情弹窗、审批弹窗、删除确认和业务阻塞提示，均不得使用浏览器默认弹窗，包括 `alert`、`confirm`、`prompt`。

所有弹窗必须使用项目 UI 规范中的统一弹窗组件或统一样式结构，并保持视觉、交互和文案风格一致。

弹窗应至少满足以下规则：

- 使用统一遮罩、弹窗容器、标题区、内容区和底部操作区。
- 主操作、取消操作、危险操作的按钮样式保持一致。
- 删除、驳回、关闭任务等危险操作必须使用明确的二次确认弹窗。
- 表单类弹窗必须提供字段校验提示，不使用浏览器原生校验弹窗作为主要提示。
- 业务阻塞提示应说明阻塞原因和需要处理的对象，例如未完成的子零件 Gate 任务、未处理的 BOM 变更、未确认的供应商变更。
- 弹窗关闭方式、遮罩点击行为、关闭按钮位置在项目内保持一致。
- 后续新增页面或修改现有页面时，应同步替换已有浏览器默认弹窗。
